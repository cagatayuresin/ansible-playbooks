---
- name: Linux and Kubernetes Node Patch / Reboot Maintenance
  hosts: all
  serial: 1
  gather_facts: true

  vars:
    node_patch_confirm: false
    node_reboot_confirm: false
    node_reboot_always: false
    node_allow_single_node_maintenance: false
    kubernetes_node_name: "{{ ansible_facts['hostname'] }}"
    first_control_plane: "{{ (groups['master'] | default([]) + groups['singlenode'] | default([])) | first | default('') }}"
    kubernetes_inventory_hosts: >-
      {{
        (
          groups['master'] | default([])
          + groups['singlenode'] | default([])
          + groups['workers'] | default([])
        )
        | unique
        | list
      }}
    is_kubernetes_node: "{{ inventory_hostname in kubernetes_inventory_hosts }}"
    is_single_node_cluster: "{{ inventory_hostname in (groups['singlenode'] | default([])) }}"

  pre_tasks:
    - name: Ping connectivity test
      import_tasks: tasks/ping_pong.yml

    - name: Check k3s kubeconfig on the first control-plane
      become: true
      stat:
        path: /etc/rancher/k3s/k3s.yaml
      delegate_to: "{{ first_control_plane }}"
      register: node_k3s_kubeconfig
      run_once: true
      when: first_control_plane | length > 0

  tasks:
    - name: List pending package updates on Debian family
      become: true
      shell: >-
        apt-get -s -o Debug::NoLocking=1 upgrade
        | awk '/^Inst / {print}'
      environment:
        LC_ALL: C
      register: node_apt_updates
      changed_when: false
      failed_when: false
      check_mode: false
      when: ansible_facts['os_family'] == 'Debian'

    - name: List pending package updates on RedHat family
      become: true
      command: dnf -q check-update
      register: node_dnf_updates
      changed_when: false
      failed_when: node_dnf_updates.rc not in [0, 100]
      check_mode: false
      when: ansible_facts['os_family'] == 'RedHat'

    - name: Check reboot requirement
      become: true
      stat:
        path: /var/run/reboot-required
      register: node_reboot_required_before

    - name: Check reboot requirement on RedHat family
      become: true
      command: needs-restarting -r
      register: node_redhat_reboot_before
      changed_when: false
      failed_when: false
      check_mode: false
      when: ansible_facts['os_family'] == 'RedHat'

    - name: Compute update status
      set_fact:
        node_updates_available: >-
          {{
            (
              ansible_facts['os_family'] == 'Debian'
              and (node_apt_updates.stdout_lines | default([]) | length > 0)
            )
            or
            (
              ansible_facts['os_family'] == 'RedHat'
              and (node_dnf_updates.rc | default(0) == 100)
            )
          }}

    - name: Node patch and reboot preview
      debug:
        msg:
          - "Host: {{ inventory_hostname }} (Kubernetes node: {{ is_kubernetes_node | bool }})"
          - "Updates available: {{ node_updates_available | bool }}"
          - >-
            Reboot required: {{
              (node_reboot_required_before.stat.exists | default(false))
              or
              (
                ansible_facts['os_family'] == 'RedHat'
                and (node_redhat_reboot_before.rc | default(0) == 1)
              )
            }}
          - "Patch confirm: {{ node_patch_confirm | bool }}, reboot confirm: {{ node_reboot_confirm | bool }}"
          - >-
            {{
              node_apt_updates.stdout_lines
              | default(node_dnf_updates.stdout_lines | default([]))
            }}

    - name: Validate OS for patching
      assert:
        that:
          - ansible_facts['os_family'] in ['Debian', 'RedHat']
          - not (is_kubernetes_node | bool) or first_control_plane | length > 0
          - >-
            not (is_single_node_cluster | bool)
            or (node_allow_single_node_maintenance | bool)
        fail_msg: >-
          Unsupported OS, control-plane not found, or single-node
          maintenance was not explicitly confirmed.
      when: node_patch_confirm | bool

    - name: Confirmed node maintenance flow
      when:
        - node_patch_confirm | bool
        - node_updates_available | bool
          or node_reboot_confirm | bool
      block:
        - name: Drain Kubernetes node
          become: true
          environment:
            KUBECONFIG: >-
              {{
                '/etc/rancher/k3s/k3s.yaml'
                if (node_k3s_kubeconfig.stat.exists | default(false))
                else '/etc/kubernetes/admin.conf'
              }}
          command:
            argv:
              - kubectl
              - drain
              - "{{ kubernetes_node_name }}"
              - --ignore-daemonsets
              - --delete-emptydir-data
              - --force
              - --timeout=300s
          delegate_to: "{{ first_control_plane }}"
          register: node_drain_result
          changed_when: true
          when: is_kubernetes_node | bool

        - name: Upgrade all packages on Debian family
          become: true
          apt:
            update_cache: true
            cache_valid_time: 3600
            upgrade: dist
          when: ansible_facts['os_family'] == 'Debian'

        - name: Upgrade all packages on RedHat family
          become: true
          dnf:
            name: "*"
            state: latest
            update_only: true
          when: ansible_facts['os_family'] == 'RedHat'

        - name: Check reboot requirement after updates
          become: true
          stat:
            path: /var/run/reboot-required
          register: node_reboot_required_after

        - name: Check reboot requirement after updates on RedHat family
          become: true
          command: needs-restarting -r
          register: node_redhat_reboot_after
          changed_when: false
          failed_when: false
          check_mode: false
          when: ansible_facts['os_family'] == 'RedHat'

        - name: Reboot node when confirmed
          become: true
          reboot:
            reboot_timeout: 900
            connect_timeout: 10
            post_reboot_delay: 20
          when:
            - node_reboot_confirm | bool
            - node_reboot_always | bool
              or node_reboot_required_after.stat.exists | default(false)
              or
              (
                ansible_facts['os_family'] == 'RedHat'
                and (node_redhat_reboot_after.rc | default(0) == 1)
              )

        - name: Uncordon Kubernetes node
          become: true
          environment:
            KUBECONFIG: >-
              {{
                '/etc/rancher/k3s/k3s.yaml'
                if (node_k3s_kubeconfig.stat.exists | default(false))
                else '/etc/kubernetes/admin.conf'
              }}
          command:
            argv:
              - kubectl
              - uncordon
              - "{{ kubernetes_node_name }}"
          delegate_to: "{{ first_control_plane }}"
          changed_when: true
          when: is_kubernetes_node | bool

      rescue:
        - name: Attempt to uncordon Kubernetes node after failure
          become: true
          environment:
            KUBECONFIG: >-
              {{
                '/etc/rancher/k3s/k3s.yaml'
                if (node_k3s_kubeconfig.stat.exists | default(false))
                else '/etc/kubernetes/admin.conf'
              }}
          command:
            argv:
              - kubectl
              - uncordon
              - "{{ kubernetes_node_name }}"
          delegate_to: "{{ first_control_plane }}"
          changed_when: true
          failed_when: false
          when:
            - is_kubernetes_node | bool
            - node_drain_result is defined
            - node_drain_result is not skipped

        - name: Report node maintenance failure
          fail:
            msg: >-
              {{ inventory_hostname }} maintenance flow failed. Uncordon was
              attempted if possible; check node and workload status manually.
