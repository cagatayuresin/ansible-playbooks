---
# Ortak: metrics-server yoksa resmi manifest ile kurar, gerekirse insecure-tls ekler.
# Beklenen play vars: first_control_plane, metrics_server_manifest_url,
#                      metrics_server_kubelet_insecure_tls
# Önce metrics_server_check çalışmış olmalı (metrics_server_ready fact).

- name: "metrics-server zaten hazır, kurulum atlanıyor"
  debug:
    msg: "metrics-server zaten kurulu ve API Available — kurulum yapılmadı."
  when: metrics_server_ready | default(false) | bool

- name: "metrics-server manifestini uygula (Çalışma Şartı: kurulu/hazır OLMAMALI)"
  command: kubectl apply -f {{ metrics_server_manifest_url }}
  register: metrics_server_apply
  changed_when: true
  when: not (metrics_server_ready | default(false) | bool)

- name: "kubelet-insecure-tls argümanını ekle (Çalışma Şartı: kurulum yapıldı ve flag açık)"
  shell: |
    set -e
    args="$(kubectl -n kube-system get deploy metrics-server -o jsonpath='{.spec.template.spec.containers[0].args}')"
    if echo "$args" | grep -q -- '--kubelet-insecure-tls'; then
      echo "kubelet-insecure-tls zaten var"
      exit 0
    fi
    kubectl -n kube-system patch deployment metrics-server --type='json' \
      -p='[{"op":"add","path":"/spec/template/spec/containers/0/args/-","value":"--kubelet-insecure-tls"}]'
  register: metrics_server_patch
  changed_when: "'patched' in (metrics_server_patch.stdout | default(''))"
  when:
    - not (metrics_server_ready | default(false) | bool)
    - metrics_server_kubelet_insecure_tls | default(true) | bool
    - metrics_server_apply is defined
    - metrics_server_apply is not skipped
    - metrics_server_apply.rc | default(1) == 0

- name: "metrics-server rollout tamamlanmasını bekle (Çalışma Şartı: kurulum yapıldı)"
  command: kubectl -n kube-system rollout status deployment/metrics-server --timeout=180s
  register: metrics_server_rollout
  changed_when: false
  when:
    - not (metrics_server_ready | default(false) | bool)
    - metrics_server_apply is defined
    - metrics_server_apply is not skipped
    - metrics_server_apply.rc | default(1) == 0

- name: "kubectl top hazır olana kadar dene (Çalışma Şartı: kurulum yapıldı)"
  command: kubectl top nodes
  register: metrics_top_wait
  retries: 12
  delay: 10
  until: metrics_top_wait.rc == 0
  changed_when: false
  ignore_errors: true
  check_mode: false
  when:
    - not (metrics_server_ready | default(false) | bool)
    - metrics_server_apply is defined
    - metrics_server_apply is not skipped
    - metrics_server_apply.rc | default(1) == 0
